# finall_all_front_0516 目录说明

这个目录承载当前主前端和历史模板。真正运行中的 FinSight 主前端位于：

```text
/home/maoyd/finsight/finall_all_front_0516/front
```

## 目录

```text
finall_all_front_0516/
  front/           当前主 React + Vite 前端
  fron_template/   历史模板/参考目录
```

## 当前主前端

进入 `front/` 启动：

```bash
cd /home/maoyd/finsight/finall_all_front_0516/front
npm install
npm run dev -- --host 0.0.0.0 --port 5173
```

详细说明见：

```text
front/README.md
```

## 注意

项目根目录还有一个 `front/`，那是旧单页聊天 HTML。当前工作台页面、法务合规页、专业 agent 头像和 Vite 代理配置都在本目录的 `front/` 下。

